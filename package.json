{
  "name": "ffmpeg-convert-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --concurrency=20",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test": "turbo run test --parallel"
  },
  "devDependencies": {
    "@turbo/gen": "^2.0.12",
    "eslint": "^8.57.0",
    "jsdom": "^26.1.0",
    "prettier": "^3.3.2",
    "turbo": "^2.0.12"
  },
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
  },
  "packageManager": "pnpm@9.10.0"
}
